# PE Ultimate

ערכת שטח למורה לחינוך גופני — אפליקציית דפדפן אחת, בלי התקנה ובלי שרת.
כל הנתונים נשמרים במכשיר בלבד (`localStorage` + `IndexedDB` לסרטונים) —
שום דבר לא נשלח לשום שרת.

> **שכפול עצמאי.** PE Ultimate הוא שכפול (fork) עצמאי ומלא של
> [`osher15/Pro-sport-pa`](https://github.com/osher15/Pro-sport-pa)
> (המגרש PRO) — קוד כמעט זהה, היסטוריה משותפת, אבל **ריפו נפרד,
> זהות נתונים נפרדת, אירוח נפרד.** שינוי כאן לא נוגע במגרש PRO,
> ולהפך. ראו [`docs/ORIGIN_VISION.md`](docs/ORIGIN_VISION.md) לניתוח
> השוק והחזון שממנו הפרויקט הזה יצא לדרך.

---

## מה יש כאן

- **‎30‎ מבחני כושר** — ריבוי ניסיונות, כרטיס תלמיד, מדד כושר, נורמות בית־ספריות
- **פוטו־פיניש** — מדידת זמנים מהמצלמה, ברזולוציה של מילישניות
- **ביפ טסט** — פרוטוקול Léger מלא, VO₂max, נורמות FITNESSGRAM
- **מערכי שיעור** — מחולל מהיר + בונה ידני בחמישה שלבים
- **ציונים ותלמידים** — רשימות כיתה, מבנה ציון, קבוצות הוראה
- **שיאים, משחקים, מאגר ידע** — ותוכן מקצועי מצוטט, לא "בערך"
- **PWA אופליין מלא** — נפתח במגרש בלי קליטה, גם כקובץ יחיד (`Hamegrash.html`) וגם כאפליקציה מותקנת
- **ארבע שפות** — עברית (ברירת מחדל) · English · العربية · Русский, עם RTL/LTR אוטומטי

## פרטיות וגיבוי

אין חשבון, אין שרת, אין שליחה החוצה. הכול חי ב-`localStorage`/`IndexedDB`
של המכשיר, בדיוק כמו במגרש PRO. גיבוי לקובץ (מוצפן, רשות) הוא הדרך
היחידה להעביר נתונים בין מכשירים או לגבות אותם — ראו את ה-README המקורי
של המגרש PRO לפירוט המלא של מנגנון ההצפנה וההתקנה כ-PWA; ההתנהגות זהה
כאן אחד לאחד.

**גיבוי לגוגל דרייב (רשות):** אותו מנגנון בדיוק, לתיקייה נפרדת —
`"PE Ultimate – Backups"` ולא `"המגרש PRO – גיבויים"`. אם אתם מגדירים
Client ID משלכם ל-OAuth, זכרו להוסיף את הכתובת שבה PE Ultimate מתארח
(למשל `https://pe-ultimate.netlify.app`) תחת **Authorized JavaScript
origins** — לא את הכתובת של המגרש PRO.

## למה זה ריפו נפרד, לא רק שם אחר

שתי אפליקציות שחולקות origin (כתובת-בסיס) חולקות גם את `localStorage`,
`IndexedDB` ואת מטמון האופליין של הדפדפן — **גם אם הקוד יושב בשני
ריפואים שונים.** PE Ultimate פותר את זה בשתי שכבות:

1. **origin נפרד** — מתארח בנפרד מהמגרש PRO (לא תחת אותו דומיין),
   כדי שההפרדה תהיה מובטחת גם בלי תלות בקוד.
2. **מזהים שונים בקוד**, גם ליתר ביטחון וגם למקרה שבו שתי האפליקציות
   ירוצו אי־פעם מקובץ מקומי על אותו מכשיר: תחילית `localStorage` שונה
   (`peultimate.` ולא `pehub.`), מסד `IndexedDB` שונה, תחילית מטמון
   שונה ב-service worker, ותיקיית דרייב נפרדת. כל אלה מרוכזים בקובץ
   אחד — `hm-brand.js` — כדי ששינוי עתידי לא יצטרך לגעת בעשרות מקומות.

תוכנית ההפרדה המלאה, כולל כל ההתנגשויות שנבדקו והוחלט עליהן, תועדה
בריפו המקורי (`docs/FORK_PLAN.md` ב-`osher15/Pro-sport-pa`).

## הפעלה

```bash
python3 -m http.server 8000
# ואז http://localhost:8000
```

או פשוט לפתוח את **`Hamegrash.html`** — קובץ יחיד שעובד בלחיצה כפולה,
בלי שרת ובלי אינטרנט.

לבנייה מחדש של הקובץ היחיד אחרי שינוי בקוד:

```bash
node build-standalone.js
```

לאימות מלא (בנייה + כל הבדיקות):

```bash
npm run verify
```

## פריסה

הריפו הזה מתארח דרך **Netlify**, לא GitHub Pages — כדי לקבל כתובת
(origin) שונה לגמרי מ-`osher15.github.io`, ולא רק תיקייה אחרת תחת
אותה כתובת. ב-Netlify: **Import an existing project → Deploy with
GitHub**, ולא "בנה לי אתר" — הקוד כבר קיים כאן, אין צורך לייצר אותו
מחדש. אין שלב build מיוחד: הריפו עצמו הוא האתר הסטטי (`index.html`
בשורש).

## מבנה

| קובץ | תפקיד |
| --- | --- |
| `index.html` | הממשק המלא (כל המסכים) |
| `hm-brand.js` | זהות המוצר בלבד: תחילית האחסון, שם מסד השיאים, תיקיית הגיבוי בדרייב. שכפול נוסף של האפליקציה משנה קובץ אחד — את זה |
| `hm-app.js` | ליבה, ראוטר, הרשאות + מודולי ביפ / פוטו־פיניש / שיאים / כושר |
| `hm-know.js` | מאגר הידע והמקורות + דף המשחקים |
| `hm-plans.js` | ארכיון מערכי השיעור המובנה |
| `hm-tools.js` | כלי כיתה: קבוצות, הגרלה, נוכחות, מחוונים |
| `hm-qr.js` | מקודד QR עצמאי |
| `hm-howto.js` | ברירת המחדל של הענפים + כללי הביצוע והצילום |
| `hm-lesson.js` | מחולל מערכי השיעור (המצב המהיר) |
| `hm-build.js` | הבונה הידני של מערך השיעור |
| `hm-tests.js` | מבחני כושר: קטלוג, בחירת כיתה, שעון/מונה, רשימות |
| `hm-new.js` | תלמידים / תזונה / מסך כניסה ותוספות בית |
| `hm-i18n.js` | ‎4‎ שפות ומנגנון ה-RTL/LTR |
| `hm-data.js` | שכבת הנתונים הטהורה — סכמה, מיגרציות, גיבוי. נבדקת גם ב-Node |
| `hm-styles.css` | עיצוב |
| `exercise-gifs/` | GIFים של ספריית התרגילים + קרדיטים |
| `sync-backend/Code.gs` | סנכרון שיאים בין מורים (Google Apps Script) — **דורש deployment וגיליון נפרדים משל המגרש PRO**, לא אותם |
| `support.js` | ריצת הרכיב (לגרסת `index.html`) |
| `build-standalone.js` | בונה את `Hamegrash.html` |
| `Hamegrash.html` | קובץ יחיד אופליין — נבנה, לא נערך ידנית |
| `docs/ORIGIN_VISION.md` | מסמך חזון מקורי, ירושה מהמגרש PRO — עדיין ראוי לקריאה |
| `docs/PRODUCT_EVOLUTION_PLAN.md` | תוכנית שלב אפס — הביקורת שממנה נבנו מערכת השעות, «היום שלי», סיום שיעור עם תוצאה, מסך הכיתה וההמשך המומלץ. ‎§13‎ מביא את מצב היישום כאן ואת מה שעדיין פתוח |
| `docs/i18n-glossary.md` | מילון מונחים מקצועיים (חינוך גופני) באנגלית/ערבית/רוסית, עם מקור לכל מונח — תנאי סף לתרגום מאגר הידע ומערכי השיעור |
| `tests/` | ‎511‎ בדיקות יחידה (`hm-data.js`, Node) + ‎417‎ בדיקות דפדפן (Playwright) |

## מה שונה מהמגרש PRO, בפועל

| | המגרש PRO | PE Ultimate |
| --- | --- | --- |
| שם ומיתוג | המגרש PRO | PE Ultimate |
| תחילית אחסון | `pehub.` | `peultimate.` |
| מסד IndexedDB | `pehub-records` | `peultimate-records` |
| תחילית מטמון | `hamegrash-` | `peultimate-` |
| תיקיית גיבוי בדרייב | "המגרש PRO – גיבויים" | "PE Ultimate – Backups" |
| אירוח | GitHub Pages | Netlify |
| שאר הקוד | — | זהה כמעט לחלוטין |

מעבר לזה — **שום דבר לא שונה**: אותם מבחנים, אותה לוגיקת ניקוד,
אותה שכבת נתונים, אותם בדיקות. זו נקודת הפתיחה, לא נקודת הסיום.

## הערות

- **אין קוד ברירת מחדל.** בכניסה הראשונה בכל מכשיר המסך מבקש לקבוע
  קוד, והוא נשמר ב-`localStorage` של אותו מכשיר בלבד.
- כל הנתונים נשמרים מקומית במכשיר. אין שרת, אין חשבון, אין שליחה
  החוצה.
- דורש דפדפן מודרני. מצלמה ומיקרופון דורשים HTTPS או `localhost`.

## רישיון ותוכן

תוכן טקסטואלי ולוגיקה — ירושה מ-`osher15/Pro-sport-pa`. קרדיטים
לתוכן חזותי (תמונות/GIF) ב-`exercise-gifs/CREDITS.md`.
