# תוכנית אבולוציית המוצר — «מערכת ההפעלה למורה לחינוך גופני»

**שלב 0 — ביקורת בלבד. אפס שינויי קוד במסמך הזה.**
נכתב על `32872b7`, ענף `claude/phase-8-identity-closure-zdfogy`, בריפו
המקורי (`osher15/Pro-sport-pa`).

> **למה המסמך הזה נמצא כאן.** PE Ultimate ירש את הקוד של המגרש PRO
> אבל לא את הזיכרון שמסביר אותו. זאת תוכנית שלב אפס — המסמך שקבע מה
> ייבנה מעל שכבת הנתונים הקיימת, ושלפיו נבנו מערכת השעות, «היום
> שלי», סיום שיעור עם תוצאה, מסך הכיתה וההמשך המומלץ שנמצאים כאן
> בקוד היום.
>
> **גוף המסמך נשאר כפי שנכתב** — הוא ביקורת מתוארכת, ותיקון שלו
> בדיעבד היה הופך אותו לתיאור של ההווה במקום לתיעוד של ההחלטה.
> לכן המספרים, שמות הקבצים והתחילית `pehub.` שבגוף המסמך הם של
> המגרש PRO בזמן הכתיבה. **מה נכון כאן, היום, נמצא ב-§13.**

החזון הוא לחבר **תכנון → הוראה → מדידה → תלמידים → התקדמות → בית ספר**.
המסמך הזה עונה על שאלה צרה בהרבה: *מה כבר קיים, מה חסר, ומה השינוי
הקטן ביותר שסוגר את הפער.*

המסקנה מראש, כדי לא לקבור אותה: **רוב שכבת הנתונים של זרימת העבודה
כבר בנויה ובדוקה.** `LessonSession`, זהות תלמיד וכיתה, מדידות, הערכה,
התקדמות ופרופיל — כולם קיימים מהשלבים 4–11. מה שחסר הוא **הקצה
העליון**: אין מושג של «השיעורים שלי היום», דף הבית לא יודע דבר על
הוראה, ולשיעור שהסתיים אין תוצאה מתועדת. זה הפער האמיתי, והוא קטן
בהרבה ממה שהוא נראה.

---

## 1. מה כבר עובד (A)

| תחום | מצב | איפה |
|---|---|---|
| **זהות תלמיד** | `sid` יציב, סיומת סדר לשמות כפולים, הכרעת מדידות דו-משמעיות | `hm-data.js` |
| **זהות כיתה** | `cid` יציב (`c:ט:3`), רישום `ft.classes`, שינוי שם בלי לנתק היסטוריה | `hm-data.js`, `hm-app.js` |
| **`LessonSession`** | מודל מלא וטהור: `createSession` / `completeSession` / `resumeSession` / `listSessions` / `sessionMeasurements`. הגנת כפילות (`created` / `resumed` / `blocked`) | `hm-data.js:1211-1315` |
| **הקשר שיעור** | בורר הכיתה, הנוכחות ושליחת התוצאות נפתחים על כיתת השיעור הפעיל | `hm-tests.js`, `hm-tools.js` |
| **מדידה** | 30 מבחנים, שעון ומונה, ניסיונות מרובים, `sessionId` נושא הקשר | `hm-tests.js` |
| **הערכה** | `assess()` מחזירה ציון **או סיבה**, גרסאות ניקוד, ארכוב נורמות | `hm-data.js` |
| **התקדמות** | `progress()`, שיא אישי עם כיוון, מגמה, «מה חסר» | `hm-data.js` |
| **פרופיל תלמיד** | `profileOf()` — היסטוריה חוצת כיתות, שיא מול אחרון | `hm-data.js`, `hm-tests.js` |
| **נוכחות** | לפי תאריך + כיתה, ארבעה סימונים, דוח CSV, אחוז השתתפות | `hm-tools.js` |
| **היסטוריית שיעורים** | חלון `lsHistModal` — רשימה + מדידות של כל שיעור | `hm-app.js:610` |
| **אופליין** | הכול ב-`localStorage` תחת `pehub.`, `IndexedDB` לווידאו, PWA מותקן, גיבוי מוצפן | `hm-app.js`, `sw.js` |
| **בדיקות** | 353 יחידה + 217 דפדפן, CI חוסם, בנייה דטרמיניסטית | `tests/` |

---

## 2. מה מומש חלקית (B)

| | מה יש | מה חסר |
|---|---|---|
| **דף הבית** | ארבעה אריחי מודולים, פס נתונים, אתגר שבועי, טיפ | **אינו יודע דבר על הוראה.** אין «היום שלי», אין «התחל שיעור», אין השיעור האחרון. `qsStu` מתעדכן רק אם מודול התלמידים נטען |
| **פתיחת שיעור** | עובדת משני מקומות: בורר הכיתה במבחני הכושר, ומתוך מערך שיעור | שניהם **בתוך מודול**. מורה שפותח את האפליקציה במגרש צריך 2–3 מסכים לפני שהוא מתחיל ללמד |
| **סיום שיעור** | `confirm()` אחד. הרשומה מסומנת `completed` | **השיעור מסתיים בלי תוצאה.** אין דירוג, אין הערה, אין «מה הלאה» — ולכן ההיסטוריה יודעת *שהיה* שיעור, לא *מה קרה בו* |
| **משוב על מערך** | `ls.feedback` — 👍/😐/👎 + הערה | ממופתח **נושא + שכבה**, לא כיתה ולא שיעור. שני מקורות אמת חלקיים במקום אחד |
| **היסטוריה** | רשימה גלובלית בחלון | **אין תצוגה לפי כיתה.** «מה עשינו בח׳2 בחודש האחרון» אינה שאלה שאפשר לשאול |
| **המשך מומלץ** | `feedbackScore()` מטה את בחירת הווריאציה בגנרטור | לא נראה למורה, ואינו נשען על השיעורים שהתקיימו בכיתה הזאת |

---

## 3. מה קיים לכל ישות

| ישות | מודל | ממשק | פער |
|---|---|---|---|
| כיתה | ✅ `cid` + רישום | חלקי — בורר, שינוי שם | **אין «מסך כיתה»** |
| תלמיד | ✅ `sid` + `cid` | ✅ כרטיס ופרופיל | — |
| מערך שיעור | ✅ `ls.lib` | ✅ גנרטור, ספרייה, הדפסה | — |
| **שיעור שהתקיים** | ✅ `LessonSession` | חלקי — פס + חלון היסטוריה | **אין תוצאה, אין מסך** |
| נוכחות | ✅ `tools.att` | ✅ | לא מקושרת לשיעור (**החלטה מכוונת**) |
| מדידה | ✅ עם `sessionId` | ✅ | — |
| הערכה | ✅ `assess()` | ✅ | 5 נקודות חישוב כפול |
| היסטוריה | ✅ `listSessions` | חלקי | **לא לפי כיתה** |
| דשבורד | ❌ | ❌ | דף הבית אינו דשבורד |
| **מערכת שעות** | ❌ | ❌ | **לא קיים כלל** — זה שורש «השיעורים שלי היום» |

---

## 4. כפילויות

1. **שתי רשימות תלמידים** — `stu.list` («התלמידים שלי») ו-`ft.roster`
   (רשימות כיתה). מגושרות ב-`importFromStu`, לא מאוחדות. **לא לגעת
   עכשיו** — איחוד הוא מיגרציה, לא חיווט.
2. **שני ערוצי משוב** — `ls.feedback` (לפי נושא) ואין משוב על השיעור
   עצמו. הפער הזה הוא הזדמנות: השיעור הוא המקום הנכון.
3. **חמש נקודות חישוב ניקוד כפול** — מתועדות ב-`STATUS.md §6.2`.
   מחוץ להיקף.
4. **שני נתיבי «התחל שיעור»** — `hm-tests.js:2195` ו-`hm-lesson.js:470`.
   לוגיקה דומה, שני מימושים. נתיב שלישי מדף הבית חייב **לקרוא לאותו
   `SESSION.start`**, לא להעתיק אותו.

---

## 5. מה שברירי טכנית

| | הסיכון |
|---|---|
| `ft.results` גדלה לנצח | יש אזהרת מכסה; אין ארכוב |
| אין מנגנון אירועים בין מודולים | ההקשר נקרא בכניסה למסך. דף בית שיציג שיעור פעיל חייב להיצבע ב-`go("home")`, אחרת יציג מצב ישן |
| `profileOf` — עד 30 מעברים לכרטיס | מסך שיקרא לו בלולאה על כיתה שלמה יאט. **מסך כיתה חייב להימנע מזה** |
| `SESSION_MAX=300` | היסטוריית שיעורים נגזמת. מספיק לשנתיים; צריך להיות מודע |
| דף הבית נטען לפני מודולים אחרים | `homeStats` לא יכול להישען על `window.STU` שאולי לא אותחל |

---

## 6. מה לא לגעת

- **שכבת הזהות** (`cidOfStudent`, `resolveClassId`, `rowInClass`,
  `renameClass`) — נסגרה בשלבים 8–9, מכוסה ב-19+17 בדיקות. **סגורה.**
- **המיגרציות** 1→4 — רצו על נתונים אמיתיים.
- **מסלול הגיבוי וההצפנה** — כישלון שקט בו עולה שנה של מדידות.
- **מנוע הביפ ושכבת התזמון של הפוטו־פיניש** — דיוק נמדד בשלב 15.
- **נוכחות שאינה נקשרת ל-`sessionId`** — החלטה מכוונת (`STATUS.md §7`):
  קשירה הייתה יוצרת שתי תשובות לשאלה «מי נכח ביום שלישי».
- **`session.measurements[]`** — לא קיים בכוונה. מדידה היא רשומה
  עצמאית שנושאת `sessionId`. מקור אמת אחד.
- **`tochnit-shnatit/`** — אפליקציה נפרדת, מחוץ להיקף.

---

## 7. השינוי הארכיטקטוני הקטן ביותר שנדרש

**שלוש תוספות. אפס מיגרציות. אפס שינוי בישויות קיימות.**

### 7.1 מערכת שעות — מפתח חדש `sched.week`

```
[ {id, day:0-6, time:"09:00", cid, clsSnapshot, topic:""} , ... ]
```

מפתח **חדש**, ולכן אין מיגרציה ואין תאימות לאחור לשבור. הלוגיקה
(מיון, «מה היום», «מה הבא», התאמה לשיעור שהתקיים) הולכת ל-`hm-data.js`
כפונקציות טהורות — כמו כל שאר המודל — ולכן ניתנת לבדיקת יחידה.

זה השורש שפותר את «מה אני עושה עכשיו». בלעדיו דף הבית יכול להציג
רק כיתות אחרונות, וזה ניחוש ולא מערכת.

### 7.2 תוצאת שיעור — שלושה שדות על רשומה קיימת

`completeSession` תקבל `{rating, note}` ותכתוב אותם על הרשומה.
שיעורים ישנים בלי השדות האלה נשארים תקפים — קריאה מגוננת, לא מיגרציה.

זה מה שהופך «התקיים שיעור» ל«**קרה** משהו בשיעור», וזה גם מה שמזין
את ההמלצה.

### 7.3 המלצה מבוססת-כללים — פונקציה טהורה

`nextLesson(sessions, opts)` — מקבלת את שיעורי הכיתה ומחזירה הצעה.
בלי API, בלי רשת, בלי מפתח. `hm-data.js`, בדיקות יחידה.

---

## 8. שלבי היישום

| שלב | מה | קבצים |
|---|---|---|
| **1** | מערכת שעות: מודל טהור + עריכה | `hm-data.js`, `hm-app.js`, `index.html` |
| **2** | דף הבית «היום שלי» — שיעורי היום, התחל שיעור, השיעור האחרון | `hm-app.js`, `index.html`, `hm-styles.css` |
| **3** | סיום שיעור עם תוצאה | `hm-data.js`, `hm-app.js`, `index.html` |
| **4** | מסך כיתה — שיעורים, מדידות, הערות | `hm-app.js`, `index.html` |
| **5** | «המשך מומלץ» | `hm-data.js`, `hm-app.js` |
| **6** | ליטוש, בדיקות, בנייה, דוח | `tests/`, `docs/` |

כל שלב נסגר בקומיט עצמאי, אחרי הרצת שתי החבילות ובנייה.

---

## 9. סיכונים

1. **דף הבית הוא המסך שכולם רואים.** רגרסיה בו נראית מיד. מיטיגציה:
   לא מוחקים דבר — האריחים, האתגר והטיפ נשארים; רק נדחקים מטה.
2. **שלושה נתיבי «התחל שיעור»** עלולים להיפרד. מיטיגציה: כולם
   קוראים ל-`SESSION.start` היחיד, ובדיקה מאמתת שפתיחה מדף הבית
   מחזירה `resumed` על שיעור פתוח.
3. **המלצה שנשמעת חכמה ואינה** — גרועה מהיעדר המלצה. מיטיגציה:
   הכללים מוצגים («כי בשיעור הקודם סימנת…»), ובלי היסטוריה אין המלצה.
4. **מערכת שעות היא עוד משהו למלא.** מורה שלא מילא אותה חייב לקבל
   דף בית שימושי בדיוק כמו קודם. מיטיגציה: מצב ריק שמציע פעולה
   אחת, ולא מסך חסום.
5. **`localStorage`** — מפתח נוסף. קטן (עשרות רשומות), ונכנס לגיבוי
   אוטומטית דרך `bkKeys()`.

---

## 10. מחוץ להיקף — במפורש

לא נבנה עכשיו, ומתועד כמפת דרכים: **שרת וסנכרון ענן · התחברות
Google/Microsoft · דשבורד בית ספר · מרחב עבודה רב-מורים · ניהול
מחוז · מרקטפלייס · רשת חברתית · אנליטיקה מתקדמת · AI מבוסס API ·
ספריית פיתוח מקצועי · חבילות תוכנית לימודים בינלאומיות · תשלומים
ומנויים · פרסומות · אפליקציות נייטיב · עיצוב מחדש · גיימיפיקציה.**

וכן, מתוך החוב הקיים ולא ייגע בו עכשיו: **איחוד `stu.list` עם
`ft.roster` · ארכוב `ft.results` · חמש נקודות חישוב הניקוד הכפול ·
מנגנון אירועים בין מודולים.**

---

## 11. מה נלקח מ-`tochnit-shnatit/` — ומה לא

האפליקציה השנייה בריפו (**תוכנית שנתית**) מחזיקה מושגי מוצר חזקים:
«היום», גאנט שנתי, שיעורים, אות הכושר, אתגרים, למידה ומערכת שעות.
היא **לא מוזגה ולא תמוזג** — היא אפליקציה נפרדת, וזה נשאר נכון.

מה שכן נלקח הוא **הלוגיקה, לא המסכים**:

| מושג | מה נלקח | למה דווקא זה |
|---|---|---|
| **«היום»** | דף הבית נפתח על שיעורי היום, לא על רשימת כלים | זאת השאלה הראשונה של מורה בשער בית הספר |
| **לוח צלצולים** | `BELLS` — עשרה שיעורים עם שעת התחלה וסיום, מועתק מ-`ap-timetable.js` | מורה חושב «שיעור שלישי», לא «09:45». הבורר ממלא את השעה |
| **«מתקיים עכשיו»** | סימון לפי השעון, בתוך חלון הצלצול | «פתוח» הוא מה שהמורה סימן; «עכשיו» הוא מה שהשעון אומר. שני דברים שונים |

**ומה שבמפורש לא נלקח:** גאנט · תוכנית שנתית · מסך למידה · מערכת
אתגרים · מסך אות הכושר · לוח שנה של חופשות · ייצוא ליומן. שום מסך
לא נוסף לניווט. המטרה הייתה שהאפליקציה תהיה **חכמה יותר**, לא
גדולה יותר.

### התפר לשכבה השנתית

שרשרת הדומיין המלאה שאליה המוצר אמור להגיע:

```
שנת לימודים → תוכנית שנתית → יחידה → מערך שיעור
   → שיעור שהתקיים → תלמידים → מדידות → תוצאות → התקדמות
```

היום מיושמים ארבעת האיברים האחרונים בלבד. שלושת העליונים ייכנסו
בלי לכתוב מחדש את הליבה, כי:

1. **`LessonSession` כבר נושא `planId`** — מערך שיעור הוא כבר ישות
   מקושרת, לא טקסט. יחידה תתחבר באותה צורה (`unitId`), בשדה נוסף
   ולא בשינוי מבנה.
2. **משבצת במערכת השעות נושאת `topic` חופשי** — כשתהיה תוכנית
   שנתית, אותו שדה יוכל לשאת מזהה יחידה במקום מחרוזת. המשבצת היא
   כבר נקודת החיבור בין «מתי» ל«מה».
3. **המדידה עצמאית ונושאת `sessionId`** — ולכן אפשר יהיה לגלגל
   תוצאות כלפי מעלה (שיעור → יחידה → תוכנית) בלי להעביר נתונים.
4. **האתגר (`hx.ch`)** הוא היום גלובלי ובלי `cid`. חיבורו לכיתה
   ולתלמידים הוא תוספת שדה, לא מודל חדש — **לא נבנה עכשיו**.
5. **אות הכושר ומדד הכושר** כבר נגזרים מתוצאות המבחנים דרך
   `assess()` ו-`progress()`. שכבת «הישגים» עתידית תישען עליהם
   ולא תשכפל נתון.


---

## 12. תיקון מהשימוש הראשון — טבלה במקום טופס

העורך הראשון של מערכת השעות ביקש **שיעור אחד בכל פעם**. זה נפל בשימוש
הראשון, ובצדק: עשרים וארבע הזנות ידניות הן בדיוק המקום שבו מורה מוותר,
והוויתור הזה מוריד לאפס את כל מה שנבנה מעל מערכת השעות.

השורש לא היה מספר השדות אלא **הצורה**: המערכת כבר קיימת אצל המורה
כטבלה של שעות מול ימים — כך היא מוצגת במשו״ב, וכך הוא חושב עליה. טופס
ליניארי מחייב אותו לתרגם אותה תוך כדי הזנה.

התיקון: אותה טבלה. עשר שורות × שישה ימים, הקשה על תא ממלאת יום ושעה,
ונשאר רק מה יש שם. בתוספת שני דברים שהתגלו כחסרים באותה בדיקה:

1. **סוג משבצת** — מערכת אמיתית של מורה לחנ״ג מכילה פרטני, שהייה
   וישיבות. מערכת שמכילה רק שיעורים מחייבת תרגום לפני ההזנה. רק
   «פרונטלי» פותח שיעור; השאר הם הקשר.
2. **מערכת לדוגמה** — שבוע מלא בלחיצה, כדי שיהיה ממה לערוך.

**באג תאימות שנתפס תוך כדי:** משבצות שנשמרו בגרסה הקודמת אין להן שדה
`kind`, ולכן אחרי העדכון הן הפסיקו להיות ניתנות לפתיחה. `kindOf()` היא
הנקודה האחת שבה משבצת הופכת לסוג, ומשבצת בלי סוג היא שיעור — כך היא
נוצרה, וכך היא ממשיכה להתנהג. מכוסה בבדיקה.

**מה שלא נעשה:** «קבוצת הוראה» — מורה שמלמד ז׳1 וז׳3 יחד מקבל שתי
משבצות באותו תא ושתי שורות בדף הבית. זה נכון לנתונים ומסורבל בממשק,
והתיקון הוא שינוי מודל. הצעד הבא הטבעי, ולא עכשיו.

---

## 13. מצב היישום ב-PE Ultimate

**נכתב ב-‎16.9.2026‎, על `0c73fff`, ענף `claude/zero-phase-program-t3jiyc`.**

הביקורת שלמעלה הגדירה שישה שלבי יישום (§8). **כל השישה סגורים בקוד
שכאן.** הם נבנו בריפו המקורי אחרי כתיבת המסמך, ו-PE Ultimate ירש
אותם במלואם בשכפול. מה שלא עבר בשכפול היה המסמך עצמו — ולכן הוא כאן.

### ‎13.1‎ איפה יושב כל שלב

| שלב | מה נבנה | איפה, בריפו הזה |
|---|---|---|
| **1** — מערכת שעות | מפתח `sched.week`, מודל טהור, טבלת עריכה של ‎10‎ שורות × ‎6‎ ימים, לוח צלצולים, סוגי משבצת, מערכת לדוגמה | `hm-data.js:1957` (`schedAdd`), `:1995` (`schedRemove`), `:2012` (`schedToday`), `:2035` (`schedNext`), `:1733` (`schedWeek`), `:1612` (`kindOf`), `:1638` (`bellByHour`) · `hm-app.js:1213` (`openSched`) · `index.html:1877` (`schedModal`) |
| **2** — «היום שלי» | שיעורי היום בדף הבית, «מתקיים עכשיו» לפי השעון, התחלת שיעור בלחיצה, השיעור האחרון, כל היום בחלון | `hm-app.js:786` (`paintToday`), `:864` (`openDay`), `:895` (`paintLastLesson`), `:708` (`startFromSlot`) · `index.html:91` (`hx-today`), `:1850` (`dayModal`) |
| **3** — סיום שיעור עם תוצאה | `completeSession` מקבלת `{rating, note}` וכותבת אותם על הרשומה; קריאה מגוננת לשיעורים ישנים | `hm-data.js:1540` (`completeSession`), `:1823` (`ratingOf`) · `hm-app.js:1271` (`openEndLesson`) · `index.html:1830` (`endModal`) |
| **4** — מסך כיתה | שיעורים, מדידות, תלמידים, הערות ודירוגים לפי כיתה — בדיוק «מה עשינו בח׳2 בחודש האחרון» | `hm-app.js:1322` (`openClassScreen`) · `index.html:1846` (`clsModal`) |
| **5** — המשך מומלץ | פונקציה טהורה, מבוססת כללים, בלי רשת ובלי מפתח; הנימוקים מוצגים למורה | `hm-data.js:1851` (`nextLesson`), `:1831` (`ladderStage`) · מוצג במסך הכיתה |
| **6** — בדיקות ובנייה | ‎59‎ בדיקות יחידה למערכת השעות, ‎22‎ להמשך המומלץ, ‎28‎ לשיעור, ‎32‎ לקבוצות; חבילת דפדפן: `today` · `week` · `lessonend` · `focus` · `groups` | `tests/unit/schedule.test.js`, `nextlesson.test.js`, `session.test.js`, `groups.test.js` · `tests/e2e/` |

**אימות בריפו הזה:** `npm run verify` — הבנייה מחזירה
`Hamegrash.html` זהה ביט-בביט, ובדיקות היחידה עוברות.
`npm run test:e2e` — בדיקות הדפדפן עוברות, ‎0‎ כשלונות. כלומר המסכים
שנבנו בשלב אפס עובדים גם אחרי החלפת תחילית האחסון ל-`peultimate.`.

### ‎13.2‎ מה בגוף המסמך כבר לא נכון כאן

| מה כתוב למעלה | מה נכון ב-PE Ultimate |
|---|---|
| «הכול ב-`localStorage` תחת `pehub.`» (§1) | התחילית היא `peultimate.`, וכל מזהי המותג מרוכזים ב-`hm-brand.js` |
| ‎353‎ יחידה + ‎217‎ דפדפן (§1) | ‎499‎ יחידה + ‎362‎ דפדפן |
| אין «מסך כיתה» (§3) | יש — `openClassScreen` |
| מערכת שעות — «לא קיים כלל» (§3) | קיימת, כולל טבלת עריכה וסוגי משבצת |
| קבוצת הוראה — «לא נעשה» (§12) | **נבנתה.** `groupOf` (`hm-data.js:148`), `makeGroup` (`:181`), `groupSummary` (`:273`), ממשק ב-`hm-app.js:1009`–`1034`, ‎32‎ בדיקות יחידה. מורה שמלמד ז׳1 וז׳3 יחד מקבל ישות אחת, והתלמידים נשארים בכיתות שלהם |
| `tochnit-shnatit/` (§6, §11) | לא קיימת בריפו הזה כלל. ההחלטה «לא למזג» נשמרה בדרך החזקה ביותר — הפרדה מלאה |

### ‎13.3‎ מה עדיין פתוח

לפי סדר התשואה, לא לפי סדר המאמץ:

1. ~~**`cid` על התלמיד — איחוד `stu.list` עם `ft.roster`.**~~
   **נסגר בסכמה 5** (`roster-membership`). `stu.list` הוא מאגר
   התלמידים היחיד, ו-`ft.roster` מחזיקה `{cid: [sid]}` — חברוּת
   בלבד. מה שאילץ את זה בסוף לא היה סדר אלא באג: המין ישב בשני
   המאגרים והוא קובע נורמה, כלומר אותו תלמיד יכול היה להיות מנוקד
   לפי נורמת בנים במסך אחד ולפי נורמת בנות בשני. ראו
   `DATA_MODEL.md` §«שתי הרשימות — איך הן הפכו לאחת».
2. **ארכוב `ft.results`.** גדלה לנצח; יש אזהרת מכסה, אין ארכוב.
   הסיכון הוא שקט עד שהוא לא.
3. **קבוצת הוראה במבחני הכושר.** מסך הכיתה מודע לקבוצה, אבל
   המבחנים עדיין עובדים על כיתה בודדת — ולכן הכפתור מוסתר בקבוצה
   במכוון (`hm-app.js:1391`). מדידה בקבוצה נעשית דרך הכיתה עצמה.
4. **חמש נקודות חישוב ניקוד כפול.** מתועדות; לא מסוכנות, מכוערות.
5. **מנגנון אירועים בין מודולים.** היום דף הבית נצבע ידנית בכל
   `go("home")` — עובד, ושביר בדיוק במידה שבה מוסיפים מסכים.
6. **השכבה השנתית** (§11) — שנת לימודים → תוכנית שנתית → יחידה.
   התפר כבר קיים: `LessonSession` נושא `planId`, המשבצת נושאת
   `topic` חופשי, והמדידה עצמאית עם `sessionId`. תוספת שדות, לא
   מודל חדש.

**וכל מה ש-§10 הוציא מההיקף — נשאר מחוץ להיקף.**
